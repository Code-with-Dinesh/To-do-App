<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .parent{
            height: 100vh;
            width: 100vw;
            background:linear-gradient(rgb(241, 175, 53),white,rgb(37, 211, 37));
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }
        ul{
            display: flex;
            flex-direction: column;

        }
        li{
            list-style-type: none;
        }
        a{
            color: blue;
            text-decoration: none;
        }
        .outer{
            border: 1px solid black;
            border-radius: 12px;
            background: lightblue;
            width: 30%;
            margin-top: 10px;
            margin-left: 10px;
            padding: 10px;
        }
        .inner{
            border: 1px solid black;
            background-color: white;
            border-radius: 12px;
            padding: 10px;
            margin: 5px;

        }
    </style>
</head>
<body>
    <div class="parent">
        <h2>This Todo Belong To @<%=userdata.username %></h2>
        <div class="outer">
        <ul>
            <% userdata.posts.forEach(function(elements){ %>
                <div class="inner">

                    <li><%= elements.desc  %></li>
                    <a href="/delete/<%=elements._id%>">Delete</a>
                </div>
                    <%   }) %>
                </ul>
        </div>
    </div>
</body>
</html>